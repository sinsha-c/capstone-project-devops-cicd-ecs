# ─── Secrets & credentials (never commit these) ─────────────────────────────
*.pem
*.key
*.p12
*.pfx
.env
.env.*
!.env.example
.aws/
credentials
*.secret

# ─── Files generated by the Jenkins pipeline / manual runs ──────────────────
# (deploy/taskdef.json and deploy/appspec.yaml ARE tracked — they are templates)
taskdef-rendered.json
appspec-rendered.yaml
revision.json
trivy-report.txt
*.tfplan
tfplan
.terraform/
*.tfstate
*.tfstate.*
*.tfvars

# ─── Editors & IDEs ──────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ─── OS files ────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ─── Logs & temp ─────────────────────────────────────────────────────────────
*.log
*.tmp
*.bak

# NOTE: screenshots/ and docs/*.png are intentionally NOT ignored —
terraform/.terraform/*
# they are the evidence and diagrams shown in the README.
invertory.ini
ansible/inventory.ini

